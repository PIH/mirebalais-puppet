petl:
  maxConcurrentJobs: 15
  homeDir: "/opt/petl"
  datasourceDir: "/opt/petl/configurations/datasources"
  jobDir: "/opt/petl/configurations/jobs"
  startup:
    jobs:
      - "create-partitions.yml"

executeCron:
  refreshOpenmrsData: "<%= @petl_cron_time -%>"
  refreshCesCiData: ""

server:
  port: "<%= @petl_server_port -%>"

logging:
  level:
    root: "WARN"
    org.pih: "INFO"

spring:
  datasource:
    platform: "mssql"
    driver-class-name: "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    url: "jdbc:sqlserver://<%= @petl_sqlserver_host -%>:<%= @petl_sqlserver_port -%>;databaseName=ces_omrs_dw"
    username: "<%= @petl_sqlserver_user -%>"
    password: "<%= @petl_sqlserver_password -%>"
  jpa:
    hibernate:
      dialect: "org.hibernate.dialect.SQLServer2012Dialect"
  liquibase:
    database-change-log-table: "petl_database_change_log"
    database-change-log-lock-table: "petl_database_change_log_lock"

datasources:
  mysql:
    user: "<%= @petl_mysql_user -%>"
    password: "<%= @petl_mysql_password -%>"
    options: "<%= @petl_mysql_options -%>"

  openmrs:
    jaltenango:
       host: "localhost"
       port: "3417"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-prod_mysql"
    planBaja:
       host: "localhost"
       port: "3416"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-plan-baja_mysql"
    planAlta:
       host: "localhost"
       port: "3415"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-plan-alta_mysql"
    salvador:
       host: "localhost"
       port: "3419"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-salvador_mysql"
    reforma:
      host: "localhost"
      port: "3418"
      databaseName: "openmrs"
      user: "${datasources.mysql.user}"
      password: "${datasources.mysql.password}"
      options: "${datasources.mysql.options}"
      containerName: "mexico_ces-reforma_mysql"
    soledad:
       host: "localhost"
       port: "3420"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-soledad_mysql"
    honduras:
       host: "localhost"
       port: "3411"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-honduras_mysql"
    capitan:
       host: "localhost"
       port: "3410"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-capitan_mysql"
    laguna:
       host: "localhost"
       port: "3412"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-laguna_mysql"
    matazano:
       host: "localhost"
       port: "3414"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-matazano_mysql"
    letrero:
       host: "localhost"
       port: "3413"
       databaseName: "openmrs"
       user: "${datasources.mysql.user}"
       password: "${datasources.mysql.password}"
       options: "${datasources.mysql.options}"
       containerName: "mexico_ces-letrero_mysql"

  warehouse:
       host: "<%= @petl_sqlserver_host -%>"
       port: "<%= @petl_sqlserver_port -%>"
       databaseName: "ces_omrs_dw"
       user: "<%= @petl_sqlserver_user -%>"
       password: "<%= @petl_sqlserver_password -%>"